[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm ci --omit=dev"]

[phases.build]
cmds = []

[start]
cmd = "npm start"

[build]
nixPkgs = ["nodejs_20"]

[metrics]
cpu = 512
memory = 1024
